
<% contentFor('head') -%>
<title>Mes Formations | ADSIAM</title>
<% contentFor('scripts') -%>

<style>
.formations-container {
    padding: 2rem;
    max-width: 1600px;
    margin: 0 auto;
}

.formations-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 3rem;
    padding: 2rem;
    background: var(--white);
    border-radius: var(--border-radius-lg);
    box-shadow: var(--shadow);
}

.header-title {
    font-size: 2.5rem;
    font-weight: 300;
    color: var(--text-dark);
    margin-bottom: 0.5rem;
}

.header-subtitle {
    color: var(--text-light);
    font-size: 1.1rem;
}

.formations-filters {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
}

.filter-btn {
    padding: 0.8rem 1.5rem;
    border: 2px solid var(--soft-gray);
    background: var(--white);
    border-radius: 25px;
    cursor: pointer;
    transition: var(--transition);
    font-weight: 500;
}

.filter-btn.active {
    background: var(--gradient-contact);
    color: white;
    border-color: transparent;
}

.formations-stats {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
}

.stat-card {
    background: var(--white);
    padding: 2rem;
    border-radius: var(--border-radius-lg);
    box-shadow: var(--shadow);
    text-align: center;
    transition: var(--transition);
}

.stat-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
}

.stat-icon {
    width: 60px;
    height: 60px;
    margin: 0 auto 1rem;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.8rem;
    color: white;
}

.stat-card:nth-child(1) .stat-icon { background: var(--gradient-contact); }
.stat-card:nth-child(2) .stat-icon { background: var(--gradient-blue); }
.stat-card:nth-child(3) .stat-icon { background: var(--gradient-green); }
.stat-card:nth-child(4) .stat-icon { background: var(--gradient-orange); }

.stat-value {
    font-size: 2rem;
    font-weight: 700;
    color: var(--text-dark);
    margin-bottom: 0.5rem;
}

.stat-label {
    color: var(--text-light);
    font-size: 0.9rem;
}

.formations-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
}

.formation-card {
    background: var(--white);
    border-radius: var(--border-radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow);
    transition: var(--transition);
    cursor: pointer;
}

.formation-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-lg);
}

.formation-header {
    height: 150px;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

.formation-card:nth-child(4n+1) .formation-header { background: var(--gradient-pink); }
.formation-card:nth-child(4n+2) .formation-header { background: var(--gradient-blue); }
.formation-card:nth-child(4n+3) .formation-header { background: var(--gradient-green); }
.formation-card:nth-child(4n+4) .formation-header { background: var(--gradient-orange); }

.formation-icon {
    font-size: 3rem;
    color: white;
}

.formation-status {
    position: absolute;
    top: 1rem;
    right: 1rem;
    padding: 0.4rem 1rem;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 600;
    backdrop-filter: blur(10px);
}

.status-en-cours {
    background: rgba(40, 167, 69, 0.9);
    color: white;
}

.status-termine {
    background: rgba(23, 162, 184, 0.9);
    color: white;
}

.status-non-commence {
    background: rgba(255, 193, 7, 0.9);
    color: var(--text-dark);
}

.formation-content {
    padding: 2rem;
}

.formation-meta {
    display: flex;
    gap: 0.8rem;
    margin-bottom: 1.5rem;
}

.meta-tag {
    padding: 0.3rem 0.8rem;
    border-radius: 15px;
    font-size: 0.8rem;
    font-weight: 600;
}

.meta-modules {
    background: var(--light-blue);
    color: var(--primary-blue);
}

.meta-duree {
    background: var(--light-pink);
    color: var(--primary-pink);
}

.formation-title {
    font-size: 1.3rem;
    font-weight: 600;
    color: var(--text-dark);
    margin-bottom: 1rem;
    line-height: 1.3;
}

.formation-progress {
    margin-bottom: 1.5rem;
}

.progress-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.5rem;
}

.progress-label {
    font-size: 0.9rem;
    color: var(--text-light);
}

.progress-percentage {
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--primary-pink);
}

.progress {
    height: 6px;
    background: var(--soft-gray);
    border-radius: 3px;
    overflow: hidden;
}

.progress-bar {
    height: 100%;
    background: var(--gradient-contact);
    border-radius: 3px;
    transition: width 1s ease;
}

.formation-actions {
    display: flex;
    gap: 1rem;
}

.btn-formation {
    flex: 1;
    padding: 0.8rem 1.5rem;
    border: none;
    border-radius: var(--border-radius);
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
    text-decoration: none;
    text-align: center;
    font-size: 0.9rem;
}

.btn-continuer {
    background: var(--gradient-contact);
    color: white;
}

.btn-continuer:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(231, 166, 183, 0.4);
}

.btn-voir {
    background: transparent;
    color: var(--primary-blue);
    border: 2px solid var(--primary-blue);
}

.btn-voir:hover {
    background: var(--primary-blue);
    color: white;
}

.empty-state {
    text-align: center;
    padding: 4rem 2rem;
    background: var(--white);
    border-radius: var(--border-radius-lg);
    box-shadow: var(--shadow);
}

.empty-icon {
    font-size: 4rem;
    margin-bottom: 2rem;
    opacity: 0.5;
}

.empty-title {
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--text-dark);
    margin-bottom: 1rem;
}

.empty-description {
    color: var(--text-light);
    margin-bottom: 2rem;
    line-height: 1.7;
}

@media (max-width: 768px) {
    .formations-container {
        padding: 1rem;
    }
    
    .formations-header {
        flex-direction: column;
        gap: 2rem;
        text-align: center;
    }
    
    .formations-grid {
        grid-template-columns: 1fr;
    }
    
    .formations-stats {
        grid-template-columns: repeat(2, 1fr);
    }
    
    .formation-actions {
        flex-direction: column;
    }
}
</style>

<div class="dashboard-layout">
    <%- include('../partials/sidebar') %>
    
    
    <main class="main-content">
        
        <%- include('../partials/header', { pageTitle: 'Mes Formations', showSearch: true }) %>

        <div class="formations-container">
            <!-- Header -->
            <div class="formations-header fade-in">
                <div>
                    <h1 class="header-title">Mes Formations</h1>
                    <p class="header-subtitle">Suivez votre progression et continuez votre apprentissage</p>
                </div>
                <div class="formations-filters">
                    <button class="filter-btn active" data-filter="all">Toutes</button>
                    <button class="filter-btn" data-filter="en_cours">En cours</button>
                    <button class="filter-btn" data-filter="termine">Terminées</button>
                    <button class="filter-btn" data-filter="non_commence">À commencer</button>
                </div>
            </div>

            <!-- Stats -->
            <div class="formations-stats">
                <div class="stat-card fade-in">
                    <div class="stat-icon">📚</div>
                    <div class="stat-value" data-count="<%= inscriptions ? inscriptions.length : 0 %>">0</div>
                    <div class="stat-label">Formations inscrites</div>
                </div>
                <div class="stat-card fade-in">
                    <div class="stat-icon">📈</div>
                    <div class="stat-value" data-count="<%= stats ? stats.enCours : 0 %>">0</div>
                    <div class="stat-label">En cours</div>
                </div>
                <div class="stat-card fade-in">
                    <div class="stat-icon">✅</div>
                    <div class="stat-value" data-count="<%= stats ? stats.terminees : 0 %>">0</div>
                    <div class="stat-label">Terminées</div>
                </div>
                <div class="stat-card fade-in">
                    <div class="stat-icon">🏆</div>
                    <div class="stat-value" data-count="<%= stats ? stats.certifications : 0 %>">0</div>
                    <div class="stat-label">Certifications</div>
                </div>
            </div>

            <!-- Formations Grid -->
            <div class="formations-grid">
                <% if (inscriptions && inscriptions.length > 0) { %>
                    <% inscriptions.forEach((inscription, index) => { %>
                        <div class="formation-card fade-in" data-status="<%= inscription.statut %>" onclick="ouvrirFormation('<%= inscription.formation.id %>')">
                            <div class="formation-header">
                                <div class="formation-icon">
                                    <%= inscription.formation.icone || '📚' %>
                                </div>
                                <div class="formation-status status-<%= inscription.statut.replace(' ', '-') %>">
                                    <% if (inscription.statut === 'en_cours') { %>
                                        🎯 En cours
                                    <% } else if (inscription.statut === 'termine') { %>
                                        ✅ Terminée
                                    <% } else { %>
                                        🆕 À commencer
                                    <% } %>
                                </div>
                            </div>
                            
                            <div class="formation-content">
                                <div class="formation-meta">
                                    <span class="meta-tag meta-modules">
                                        📖 <%= inscription.formation.nombre_modules || 0 %> modules
                                    </span>
                                    <span class="meta-tag meta-duree">
                                        ⏱️ <%= inscription.formation.duree_heures || 0 %>h
                                    </span>
                                </div>
                                
                                <h3 class="formation-title"><%= inscription.formation.titre %></h3>
                                
                                <div class="formation-progress">
                                    <div class="progress-header">
                                        <span class="progress-label">Progression</span>
                                        <span class="progress-percentage"><%= inscription.progression_pourcentage || 0 %>%</span>
                                    </div>
                                    <div class="progress">
                                        <div class="progress-bar" style="width: <%= inscription.progression_pourcentage || 0 %>%;"></div>
                                    </div>
                                </div>
                                
                                <div class="formation-actions">
                                    <% if (inscription.statut === 'termine') { %>
                                        <a href="/formation/<%= inscription.formation.id %>" class="btn-formation btn-voir">
                                            📜 Certificat
                                        </a>
                                        <a href="/formation/<%= inscription.formation.id %>/revoir" class="btn-formation btn-continuer">
                                            🔄 Revoir
                                        </a>
                                    <% } else if (inscription.statut === 'en_cours') { %>
                                        <a href="/formation/<%= inscription.formation.id %>/continuer" class="btn-formation btn-continuer">
                                            ▶️ Continuer
                                        </a>
                                        <a href="/formation/<%= inscription.formation.id %>" class="btn-formation btn-voir">
                                            📋 Détails
                                        </a>
                                    <% } else { %>
                                        <a href="/formation/<%= inscription.formation.id %>/commencer" class="btn-formation btn-continuer">
                                            🚀 Commencer
                                        </a>
                                        <a href="/formation/<%= inscription.formation.id %>" class="btn-formation btn-voir">
                                            👁️ Aperçu
                                        </a>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="empty-state fade-in" style="grid-column: 1 / -1;">
                        <div class="empty-icon">📚</div>
                        <h3 class="empty-title">Aucune formation inscrite</h3>
                        <p class="empty-description">
                            Vous n'êtes inscrit à aucune formation pour le moment.<br>
                            Découvrez notre catalogue et commencez votre apprentissage !
                        </p>
                        <a href="/catalogue" class="btn btn-primary btn-lg">
                            🔍 Découvrir les formations
                        </a>
                    </div>
                <% } %>
            </div>
        </div>
    </main>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    console.log('📚 Page Mes Formations chargée');
    
    // Filtres
    const filterButtons = document.querySelectorAll('.filter-btn');
    const formationCards = document.querySelectorAll('.formation-card');
    
    filterButtons.forEach(button => {
        button.addEventListener('click', function() {
            const filter = this.getAttribute('data-filter');
            
            // Update active button
            filterButtons.forEach(btn => btn.classList.remove('active'));
            this.classList.add('active');
            
            // Filter cards
            formationCards.forEach(card => {
                const status = card.getAttribute('data-status');
                
                if (filter === 'all' || status === filter) {
                    card.style.display = 'block';
                    card.style.animation = 'fadeInUp 0.5s ease forwards';
                } else {
                    card.style.display = 'none';
                }
            });
            
            // Analytics
            window.ADSIAM_APP.track('formations_filter', { filter: filter });
        });
    });
    
    // Animation des barres de progression
    const progressBars = document.querySelectorAll('.progress-bar');
    const progressObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                const bar = entry.target;
                const width = bar.style.width;
                bar.style.width = '0%';
                setTimeout(() => {
                    bar.style.width = width;
                }, 300);
            }
        });
    });
    
    progressBars.forEach(bar => progressObserver.observe(bar));
});

function ouvrirFormation(formationId) {
    window.location.href = `/formation/${formationId}`;
    window.ADSIAM_APP.track('formation_clicked', { formation_id: formationId });
}
</script>