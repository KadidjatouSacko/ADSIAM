<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau des Employés | Dashboard</title>
    <link rel="stylesheet" href="/css/dashboard.css">
</head>
<body>
    <div class="employee-table-container">
        <h1>Tableau des Employés</h1>
        <div class="filter-options">
            <label for="roleFilter">Filtrer par rôle:</label>
            <select id="roleFilter" onchange="filterEmployees()">
                <option value="">Tous les rôles</option>
                <option value="aide-domicile">Aide à domicile</option>
                <option value="ehpad">EHPAD</option>
                <option value="soins-infirmiers">Soins infirmiers</option>
                <option value="administration">Administration</option>
            </select>
            <label for="statusFilter">Filtrer par statut:</label>
            <select id="statusFilter" onchange="filterEmployees()">
                <option value="">Tous les statuts</option>
                <option value="active">Actif</option>
                <option value="inactive">Inactif</option>
            </select>
        </div>
        <table class="employees-table">
            <thead>
                <tr>
                    <th>Nom</th>
                    <th>Rôle</th>
                    <th>Statut</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody id="employeeTableBody">
                <!-- Les données des employés seront insérées ici dynamiquement -->
            </tbody>
        </table>
    </div>

    <script>
        function filterEmployees() {
            const roleFilter = document.getElementById('roleFilter').value;
            const statusFilter = document.getElementById('statusFilter').value;
            // Logique pour filtrer les employés en fonction des sélections
        }

        // Fonction pour charger les données des employés
        function loadEmployeeData() {
            // Logique pour récupérer et afficher les données des employés
        }

        // Appel initial pour charger les données
        loadEmployeeData();
    </script>
</body>
</html>