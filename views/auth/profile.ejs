<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Mon Profil - ADSIAM' %></title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
        
        :root {
            --primary-pink: #e7a6b7;
            --primary-blue: #a5bfd4;
            --light-pink: #fce8ec;
            --light-blue: #e8f3f8;
            --soft-gray: #f5f5f7;
            --text-dark: #2f2f2f;
            --text-light: #6f6f6f;
            --white: #ffffff;
            --gradient-contact: linear-gradient(135deg, #e7a6b7, #a5bfd4);
            --success-color: #28a745;
            --warning-color: #ffc107;
            --error-color: #dc3545;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--soft-gray);
            color: var(--text-dark);
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        .header {
            background: var(--white);
            padding: 1.5rem 0;
            margin-bottom: 2rem;
            border-radius: 20px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.08);
        }

        .header-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            display: flex;
            align-items: center;
            text-decoration: none;
            color: var(--text-dark);
        }

        .logo-icon {
            width: 40px;
            height: 40px;
            background: var(--gradient-contact);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 1rem;
            color: white;
            font-weight: 700;
        }

        .logo-text {
            font-size: 1.5rem;
            font-weight: 300;
            letter-spacing: 0.1em;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        .nav-links a {
            color: var(--text-light);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s;
        }

        .nav-links a:hover {
            color: var(--primary-blue);
        }

        .main-grid {
            display: grid;
            grid-template-columns: 300px 1fr;
            gap: 2rem;
        }

        .sidebar {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .sidebar-card {
            background: var(--white);
            border-radius: 20px;
            padding: 2rem;
            box-shadow: 0 5px 20px rgba(0,0,0,0.08);
        }

        .profile-summary {
            text-align: center;
        }

        .profile-avatar {
            width: 80px;
            height: 80px;
            background: var(--gradient-contact);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            color: white;
            font-weight: 700;
            font-size: 2rem;
        }

        .profile-name {
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .profile-email {
            color: var(--text-light);
            margin-bottom: 1rem;
        }

        .profile-status {
            display: inline-block;
            padding: 0.4rem 1rem;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
        }

        .status-actif { background: #d4edda; color: #155724; }
        .status-en_attente { background: #fff3cd; color: #856404; }
        .status-inactif { background: #f8d7da; color: #721c24; }
        .status-suspendu { background: #f8d7da; color: #721c24; }

        .nav-menu {
            list-style: none;
        }

        .nav-item {
            margin-bottom: 0.5rem;
        }

        .nav-link {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem;
            color: var(--text-light);
            text-decoration: none;
            border-radius: 12px;
            transition: all 0.3s;
            font-weight: 500;
        }

        .nav-link:hover,
        .nav-link.active {
            background: var(--light-blue);
            color: var(--primary-blue);
        }

        .content-area {
            background: var(--white);
            border-radius: 20px;
            padding: 3rem;
            box-shadow: 0 5px 20px rgba(0,0,0,0.08);
        }

        .content-header {
            margin-bottom: 2rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid var(--soft-gray);
        }

        .content-title {
            font-size: 2rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .content-subtitle {
            color: var(--text-light);
        }

        .alert {
            padding: 1rem 1.5rem;
            border-radius: 10px;
            margin-bottom: 2rem;
            font-weight: 500;
        }

        .alert-success {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .alert-error {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }

        .alert-warning {
            background: #fff3cd;
            color: #856404;
            border: 1px solid #ffeaa7;
        }

        .tabs {
            display: flex;
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .tab-button {
            padding: 0.8rem 1.5rem;
            background: var(--soft-gray);
            border: none;
            border-radius: 12px;
            color: var(--text-light);
            cursor: pointer;
            font-weight: 500;
            transition: all 0.3s;
        }

        .tab-button.active {
            background: var(--gradient-contact);
            color: white;
        }

        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: block;
        }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1.5rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group.full-width {
            grid-column: 1 / -1;
        }

        .form-label {
            display: block;
            margin-bottom: 0.8rem;
            font-weight: 600;
            color: var(--text-dark);
            font-size: 0.95rem;
        }

        .form-input {
            width: 100%;
            padding: 1rem 1.2rem;
            border: 2px solid var(--soft-gray);
            border-radius: 12px;
            font-size: 0.95rem;
            transition: all 0.3s;
            background: var(--soft-gray);
        }

        .form-input:focus {
            outline: none;
            border-color: var(--primary-blue);
            background: var(--white);
            box-shadow: 0 5px 15px rgba(165, 191, 212, 0.2);
        }

        .form-input.error {
            border-color: var(--error-color);
            background: #fff5f5;
        }

        .form-input:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        .error-message {
            color: var(--error-color);
            font-size: 0.85rem;
            margin-top: 0.5rem;
        }

        .form-actions {
            display: flex;
            gap: 1rem;
            justify-content: flex-end;
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid var(--soft-gray);
        }

        .btn {
            padding: 1rem 2rem;
            border: none;
            border-radius: 12px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            font-size: 0.95rem;
            text-decoration: none;
            display: inline-block;
            text-align: center;
        }

        .btn-primary {
            background: var(--gradient-contact);
            color: white;
            box-shadow: 0 5px 15px rgba(231, 166, 183, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(231, 166, 183, 0.4);
        }

        .btn-secondary {
            background: var(--soft-gray);
            color: var(--text-light);
            border: 2px solid var(--soft-gray);
        }

        .btn-secondary:hover {
            border-color: var(--primary-blue);
            color: var(--primary-blue);
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background: var(--soft-gray);
            padding: 1.5rem;
            border-radius: 15px;
            text-align: center;
        }

        .stat-number {
            font-size: 2rem;
            font-weight: 700;
            color: var(--primary-blue);
            display: block;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            color: var(--text-light);
            font-size: 0.9rem;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .main-grid {
                grid-template-columns: 1fr;
            }

            .sidebar {
                order: 2;
            }

            .content-area {
                order: 1;
                padding: 2rem;
            }

            .form-grid {
                grid-template-columns: 1fr;
            }

            .tabs {
                flex-wrap: wrap;
            }

            .form-actions {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <div class="header">
        <div class="header-content">
            <a href="/" class="logo">
                <div class="logo-icon">A+</div>
                <div class="logo-text">ADSIAM</div>
            </a>
            <div class="nav-links">
                <a href="/dashboard">Dashboard</a>
                <a href="/formations">Formations</a>
                <a href="/auth/profile">Profil</a>
                <a href="/auth/logout">Déconnexion</a>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="main-grid">
            <!-- Sidebar -->
            <div class="sidebar">
                <div class="sidebar-card profile-summary">
                    <div class="profile-avatar">
                        <%= user.prenom.charAt(0).toUpperCase() %><%= user.nom.charAt(0).toUpperCase() %>
                    </div>
                    <div class="profile-name"><%= user.prenom %> <%= user.nom %></div>
                    <div class="profile-email"><%= user.email %></div>
                    <div class="profile-status status-<%= user.statut %>">
                        <%= getStatusText(user.statut) %>
                    </div>
                </div>

                <div class="sidebar-card">
                    <ul class="nav-menu">
                        <li class="nav-item">
                            <a href="#informations" class="nav-link active" data-tab="informations">
                                👤 Informations personnelles
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="#security" class="nav-link" data-tab="security">
                                🔒 Sécurité
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="#activity" class="nav-link" data-tab="activity">
                                📊 Activité
                            </a>
                        </li>
                    </ul>
                </div>
            </div>

            <!-- Content Area -->
            <div class="content-area">
                <div class="content-header">
                    <h1 class="content-title">Mon Profil</h1>
                    <p class="content-subtitle">Gérez vos informations personnelles et paramètres de sécurité</p>
                </div>

                <!-- Messages Flash -->
                <% if (success && success.length > 0) { %>
                    <div class="alert alert-success">
                        <%= success[0] %>
                    </div>
                <% } %>

                <% if (error && error.length > 0) { %>
                    <div class="alert alert-error">
                        <%= error[0] %>
                    </div>
                <% } %>

                <% if (warning && warning.length > 0) { %>
                    <div class="alert alert-warning">
                        <%= warning[0] %>
                    </div>
                <% } %>

                <!-- Tabs -->
                <div class="tabs">
                    <button class="tab-button active" data-tab="informations">Informations</button>
                    <button class="tab-button" data-tab="security">Sécurité</button>
                    <button class="tab-button" data-tab="activity">Activité</button>
                </div>

                <!-- Tab Informations -->
                <div class="tab-content active" id="informations">
                    <form action="/auth/profile" method="POST">
                        <div class="form-grid">
                            <div class="form-group">
                                <label for="prenom" class="form-label">Prénom</label>
                                <input 
                                    type="text" 
                                    id="prenom" 
                                    name="prenom" 
                                    class="form-input <%= typeof errors !== 'undefined' && errors.prenom ? 'error' : '' %>" 
                                    value="<%= user.prenom %>"
                                    required
                                >
                                <% if (typeof errors !== 'undefined' && errors.prenom) { %>
                                    <div class="error-message"><%= errors.prenom %></div>
                                <% } %>
                            </div>

                            <div class="form-group">
                                <label for="nom" class="form-label">Nom</label>
                                <input 
                                    type="text" 
                                    id="nom" 
                                    name="nom" 
                                    class="form-input <%= typeof errors !== 'undefined' && errors.nom ? 'error' : '' %>" 
                                    value="<%= user.nom %>"
                                    required
                                >
                                <% if (typeof errors !== 'undefined' && errors.nom) { %>
                                    <div class="error-message"><%= errors.nom %></div>
                                <% } %>
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="email" class="form-label">Email</label>
                            <input 
                                type="email" 
                                id="email" 
                                class="form-input" 
                                value="<%= user.email %>"
                                disabled
                            >
                            <small style="color: var(--text-light);">L'email ne peut pas être modifié. Contactez le support si nécessaire.</small>
                        </div>

                        <div class="form-grid">
                            <div class="form-group">
                                <label for="telephone" class="form-label">Téléphone</label>
                                <input 
                                    type="tel" 
                                    id="telephone" 
                                    name="telephone" 
                                    class="form-input"
                                    value="<%= user.telephone || '' %>"
                                    placeholder="06 12 34 56 78"
                                >
                            </div>

                            <div class="form-group">
                                <label for="societe" class="form-label">Société</label>
                                <input 
                                    type="text" 
                                    id="societe" 
                                    name="societe_rattachee" 
                                    class="form-input"
                                    value="<%= user.societe_rattachee || '' %>"
                                    placeholder="EHPAD Saint-Martin"
                                >
                            </div>
                        </div>

                        <div class="form-actions">
                            <button type="button" class="btn btn-secondary" onclick="location.reload()">
                                Annuler
                            </button>
                            <button type="submit" class="btn btn-primary">
                                Enregistrer les modifications
                            </button>
                        </div>
                    </form>
                </div>

                <!-- Tab Security -->
                <div class="tab-content" id="security">
                    <form action="/auth/change-password" method="POST">
                        <div class="form-group">
                            <label for="ancien_mot_de_passe" class="form-label">Mot de passe actuel</label>
                            <input 
                                type="password" 
                                id="ancien_mot_de_passe" 
                                name="ancien_mot_de_passe" 
                                class="form-input <%= typeof passwordErrors !== 'undefined' && passwordErrors.ancien_mot_de_passe ? 'error' : '' %>"
                                required
                            >
                            <% if (typeof passwordErrors !== 'undefined' && passwordErrors.ancien_mot_de_passe) { %>
                                <div class="error-message"><%= passwordErrors.ancien_mot_de_passe %></div>
                            <% } %>
                        </div>

                        <div class="form-grid">
                            <div class="form-group">
                                <label for="nouveau_mot_de_passe" class="form-label">Nouveau mot de passe</label>
                                <input 
                                    type="password" 
                                    id="nouveau_mot_de_passe" 
                                    name="nouveau_mot_de_passe" 
                                    class="form-input <%= typeof passwordErrors !== 'undefined' && passwordErrors.nouveau_mot_de_passe ? 'error' : '' %>"
                                    required
                                >
                                <% if (typeof passwordErrors !== 'undefined' && passwordErrors.nouveau_mot_de_passe) { %>
                                    <div class="error-message"><%= passwordErrors.nouveau_mot_de_passe %></div>
                                <% } %>
                            </div>

                            <div class="form-group">
                                <label for="confirm_nouveau_mot_de_passe" class="form-label">Confirmer le nouveau mot de passe</label>
                                <input 
                                    type="password" 
                                    id="confirm_nouveau_mot_de_passe" 
                                    name="confirm_nouveau_mot_de_passe" 
                                    class="form-input <%= typeof passwordErrors !== 'undefined' && passwordErrors.confirm_nouveau_mot_de_passe ? 'error' : '' %>"
                                    required
                                >
                                <% if (typeof passwordErrors !== 'undefined' && passwordErrors.confirm_nouveau_mot_de_passe) { %>
                                    <div class="error-message"><%= passwordErrors.confirm_nouveau_mot_de_passe %></div>
                                <% } %>
                            </div>
                        </div>

                        <div class="form-actions">
                            <button type="button" class="btn btn-secondary" onclick="document.getElementById('security').querySelectorAll('input').forEach(i => i.value = '')">
                                Annuler
                            </button>
                            <button type="submit" class="btn btn-primary">
                                Modifier le mot de passe
                            </button>
                        </div>
                    </form>
                </div>

                <!-- Tab Activity -->
                <div class="tab-content" id="activity">
                    <div class="stats-grid">
                        <div class="stat-card">
                            <span class="stat-number">-</span>
                            <span class="stat-label">Formations suivies</span>
                        </div>
                        <div class="stat-card">
                            <span class="stat-number">-</span>
                            <span class="stat-label">Certificats obtenus</span>
                        </div>
                        <div class="stat-card">
                            <span class="stat-number">-</span>
                            <span class="stat-label">Heures de formation</span>
                        </div>
                        <div class="stat-card">
                            <span class="stat-number"><%= capitalize(user.role) %></span>
                            <span class="stat-label">Rôle</span>
                        </div>
                    </div>

                    <div style="margin-top: 2rem; padding: 2rem; background: var(--soft-gray); border-radius: 15px;">
                        <h3 style="margin-bottom: 1rem;">Informations du compte</h3>
                        <div style="display: grid; gap: 1rem;">
                            <div style="display: flex; justify-content: space-between;">