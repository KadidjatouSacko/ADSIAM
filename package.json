{
  "name": "adsiam-platform",
  "version": "1.0.0",
  "description": "Plateforme de formation pour aide à domicile et EHPAD",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "seed": "node scripts/seedDatabase.js",
    "db:sync": "node -e \"import('./models/index.js').then(({sequelize}) => sequelize.sync({force: false}))\"",
    "db:reset": "node -e \"import('./models/index.js').then(({sequelize}) => sequelize.sync({force: true}))\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "formation",
    "aide-domicile",
    "ehpad",
    "nodejs",
    "express",
    "sequelize"
  ],
  "author": "ADSIAM",
  "license": "MIT",
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "express-session": "^1.18.2",
    "helmet": "^7.1.0",
    "pg": "^8.11.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.35.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
